import 'package:flutter/material.dart';

class AppBgColdstart extends StatelessWidget {
  const AppBgColdstart({
    super.key,
  });

  @override
  Widget build(BuildContext context) {
    return Image.asset(
      'assets/img/app_bg/png/cold_start_bg.png',
      height: double.infinity,
      width: double.infinity,
    );
  }
}
